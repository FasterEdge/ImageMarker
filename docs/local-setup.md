# 在本地搭建项目

```bash
# 克隆仓库
git clone https://github.com/SkalskiP/make-sense.git

# 进入主目录
cd make-sense

# 安装依赖
npm install

# 在 localhost:3000 启动并支持热重载
npm start
```
为确保应用在本地正常运行，需要 npm `6.x.x` 和 node.js `v11.x.x` 版本。有关此问题的更多信息，请参阅 [#16][1]。

[1]: https://github.com/SkalskiP/make-sense/issues/16